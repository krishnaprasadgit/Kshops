//if(user){
   // await bcrypt.compare(userData.password,this.password).then((status)=>{
        //   if(status){
          //   console.log('login success');
           //  response.user=user
             //response.status=true
             //resolve(response)
           //}else{
           //  console.log('login failed');
           //  resolve({status:false})
          // }
    // })
   //}else{
   //  console.log('login failed');
   //  resolve({status:false})
   //}